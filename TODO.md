# TODO

- [ ] Update `DashboardHeader` to accept `weekStart` prop and derive month label from it (remove hardcoded month dependency).
- [ ] Pass `calendarDays` (or the relevant state) from `Dashboard.js` into `DashboardHeader`.
- [ ] Update `Dashboard.js` to compute month label from `weekStart` and ensure `DashboardHeader` uses it via `weekStart`.
- [ ] Verify imports/props wiring and ensure the app builds (run Metro/JS bundler if applicable).

